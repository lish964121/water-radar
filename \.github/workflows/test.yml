name: Test Push
   on: workflow_dispatch
   jobs:
     push:
       runs-on: ubuntu-latest
       steps:
         - name: Send test
           run: curl -s "https://sctapi.ftqq.com/${{ secrets.SENDKEY }}.send?title=GitHubTest&desp=ok"
